using System.Collections;
using System.Collections.Generic;
using UnityEngine;

[RequireComponent(typeof(SphereCollider))]
public class FixGateStep : QuestStep
{
    //Add that if the gate is fixed, the quest is finished
}
